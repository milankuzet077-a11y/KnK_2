# AMK Kitchen Configurator — UI (fresh start)

## Start
```bash
npm install
npm run dev
```

## Notes
- UI is mobile-first:
  - Mobile: bottom dock + full drawers (no overlap)
  - Desktop: side panels + bottom bar
- 3D canvas is a placeholder. Replace `src/engine3d/Canvas3D.tsx` with Three.js later.
